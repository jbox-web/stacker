---
name: Dockerhub Release

on:
  push:
    tags:
      - '*'

jobs:
  test:
    runs-on: ubuntu-20.04
    container:
      image: crystallang/crystal:1.2.0
    steps:
      - uses: actions/checkout@v2

      - name: Install dependencies
        run: make deps

      - name: Run tests
        run: make spec

  publish-binary:
    runs-on: ubuntu-20.04
    needs: test
    steps:
      - name: Setup Earthly
        uses: earthly/actions/setup-earthly@v1
        with:
          version: v0.5.24

      # See: https://docs.earthly.dev/docs/guides/multi-platform#github-actions
      - name: Set up QEMU
        id: qemu
        uses: docker/setup-qemu-action@v1
        with:
          image: tonistiigi/binfmt:latest
          platforms: all

      - name: Git checkout
        uses: actions/checkout@v2

      - name: Build static binary
        run: make binary-release

      - name: Publish assets
        uses: ncipollo/release-action@v1
        with:
          artifacts: "packages/*"
          token: ${{ secrets.GITHUB_TOKEN }}
