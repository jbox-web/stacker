---
my_server:
  # Network
  hostname: server1
  network:
    public:
      fqdn: server1.example.net
    private:
      ip: 10.1.0.1
      fqdn: server1.example.corp

  # Roles
  roles:
    server:
      - openntpd

  # Monitoring
  monitoring:
    disks:
      - /dev/sda
      - /dev/sdb

stacker: foo
